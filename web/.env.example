# When the web app is NOT served from the same origin as the API (e.g. browser
# hits your personal server on port 5173 but API is on :3001), set:
# VITE_API_URL=http://YOUR_SERVER:3001
#
# Leave unset for: `npm run dev` (Vite proxies /api → 3001) or SERVE_WEB=1.
